{
  "name": "sales-on-credit",
  "version": "2.4.9",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-assets.mjs",
    "check": "node --check app.js && node --check auth-bootstrap.js && node --check barcode-scanner.js && node --check clients.js && node --check debt-domain.js && node --check payment-domain.js && node --check firebase.js && node --check history-domain.js && node --check history.js && node --check products.js && node --check settings.js && node --check client-view.js && node --check sw.js && node --check scripts/build-assets.mjs",
    "test": "node --test scripts/history-domain.test.mjs scripts/debt-domain.test.mjs scripts/payment-domain.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "firebase": "10.14.1",
    "purgecss": "^7.0.0"
  },
  "overrides": {
    "undici": "6.28.0"
  }
}
